{#

/* -------------------------

Title: Component Entity Layout Group
Descr: Group wrapper for layout entity item. Passes wrapper class and items.

--------------------------- */

 #}


{% set group_base_class = 'columns' %}
{% set alignment = content.field_layout[0]['#markup'] %}
{% set group_modifiers = [alignment] %}
{# {{vardumper()}} #}
{% include "@content/molecules/_grouping.twig" with {
    items : content.field_columns,
    group_base_class : group_base_class,
    group_modifiers : group_modifiers,
    group_label : content.name
}
%}
