{#

/* -------------------------

Title: Photo Text Block
Descr: Set main wrapper for photo text item
        passess wrapperClass and content to photo-text-block.twig

--------------------------- */

#}

{# {{ kint(content) }} #}
{# {% set wrapperClass = content.field_photo_text_wrapper_class %} #}

{# THIS is Broken. need to figure out how to get raw vlaue of field_photo_text_wrapper_class not as an array to pass to group_modifiers #}

{% set wrapperClass = "featured" %}

{# {{kint(wrapperClass)}} #}
{% set group_base_class = "ptWrapper" %}
{% set group_modifiers = [ wrapperClass , 'component'] %}


{% include "@organisms/photo-text-block/photo-text-block.twig" with {
    "items" : content.field_photo_text_reference
}%}


