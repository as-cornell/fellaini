
{#
/**
 * @file
 * basic full width block with plain text links.

 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.

 */
#}
{# {{kint(content)}} #}

{% include "@organisms/link-list/_link-block.twig" with{
    heading : content.field_block_title,
    links : content.field_link,
    group_base_class : 'pageComponent',
    group_modifiers : ['linkBlock', 'full-width'],
    group_classes : ['full-width']
}%}