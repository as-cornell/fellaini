{#
  template to show a div wrapped around plain text links
  Used in - landing page slices
 #}
{# Group/container bem #}
{# {% set group_base_class = group_base_class | default('') %}
{% set group_modifiers = group_modifiers | default(['']) %}
{% set group_classes = group_classes | default(['']) %}
{% set group_classes = ['link-bar', 'full-width'] %}

<div {{bem(group_base_class,(group_modifiers),'',group_classes)}}>

  {% if pl %}
    {% include "@atoms/00-text/_heading.twig" with {
          "heading_level" : 2,
          "heading" : "Explore A&S",
          "heading_modifiers" : ['border']
      } %}
      <a href="#">Majors & Minors</a>
      <a href="#">Liberal arts opportunities</a>
      <a href="#">Award winning faculty</a>
      <a href="#">World class resources</a>
  {% else %}
  {% include "@atoms/00-text/_heading.twig" with {
        "heading_level" : 2,
        "heading" : heading,
        "heading_modifiers" : ['border']
    } %}
    {{links}}
{% endif %}
</div> #}
{% include "@organisms/link-list/_link-block.twig" with{
    heading : content.field_block_title,
    links : content.field_link,
    group_base_class : 'link-bar',
    group_classes : ['link-bar', 'full-width']
}%}
